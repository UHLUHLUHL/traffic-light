# 🚦 YOLO 기반 신호등 탐지 구현 가이드

현재의 **Haar Cascade + YCrCb** 방식에서 **YOLO(You Only Look Once)** 딥러닝 모델로 전환하기 위한 단계별 가이드입니다.
YOLO는 모양과 색상을 한 번에 학습하므로 별도의 색상 검증 로직이 필요 없고, 정확도가 훨씬 높지만 라즈베리 파이에서는 속도 최적화가 필수적입니다.

---

## 1. 모델 선정 (Model Selection)
라즈베리 파이(CPU)에서 실시간 주행을 하려면 가장 가벼운 모델을 써야 합니다.

*   **추천**: **YOLOv5 Nano (`yolov5n`)** 또는 **YOLOv8 Nano (`yolov8n`)**
*   **이유**: 정확도는 조금 낮아도 속도가 가장 빠름 (Pi 4 기준 3~10 FPS 예상, 가속기 없으면 느릴 수 있음).

---

## 2. 데이터셋 준비 (Data Preparation)

### A. 데이터 수집
*   직접 주행하면서 얻은 **영상이나 사진**이 가장 좋습니다.
*   다양한 각도, 조명, 거리에서 찍은 신호등 사진을 100~500장 정도 준비하세요.

### B. 라벨링 (Labeling)
사진 속 신호등에 박스를 치고 이름을 붙이는 작업입니다.
*   **도구**: [Roboflow](https://roboflow.com/) (웹 기반, 추천), [LabelImg](https://github.com/heartexlabs/labelImg) (로컬 프로그램).
*   **클래스(Class) 정의**:
    *   `traffic_light_red` (빨간불)
    *   `traffic_light_green` (초록불)
    *   `traffic_light_yellow` (노란불)
    *   `traffic_light_off` (꺼짐)
    *   *(이렇게 클래스를 나누면 색상 검증 코드가 아예 필요 없어집니다)*

---

## 3. 모델 학습 (Training)
라즈베리 파이에서는 학습이 불가능하므로, **PC나 Google Colab**을 사용합니다.

### Google Colab 학습 예시 (YOLOv5)
```bash
# 1. YOLOv5 설치
!git clone https://github.com/ultralytics/yolov5
%cd yolov5
!pip install -r requirements.txt

# 2. 학습 시작 (Roboflow에서 받은 데이터셋 사용)
# --img 320: 이미지 크기 (작을수록 빠름, 320 추천)
# --batch 16: 한 번에 학습할 양
# --epochs 50: 학습 반복 횟수
# --data data.yaml: 데이터셋 설정 파일
# --weights yolov5n.pt: 가장 가벼운 모델 사용
!python train.py --img 320 --batch 16 --epochs 50 --data /content/dataset/data.yaml --weights yolov5n.pt
```
학습이 끝나면 `runs/train/exp/weights/best.pt` 파일이 생성됩니다. 이 파일이 **지능을 가진 두뇌**입니다.

---

## 4. 라즈베리 파이 적용 (Inference)

### A. 환경 설정
라즈베리 파이에 필요한 라이브러리를 설치합니다.
```bash
pip install ultralytics  # YOLOv8 기준 (v5는 requirements.txt 설치)
# 또는 속도를 위해 ONNX Runtime 사용 권장
pip install onnxruntime
```

### B. 파이썬 코드 예시
`best.pt` 파일을 라즈베리 파이로 옮긴 후 아래 코드를 작성합니다.

```python
import cv2
from ultralytics import YOLO

# 1. 모델 로드 (학습된 best.pt 파일)
model = YOLO("best.pt") 

cap = cv2.VideoCapture(0)
cap.set(3, 320) # 해상도를 낮춰야 빨라집니다
cap.set(4, 240)

while True:
    ret, frame = cap.read()
    if not ret: break

    # 2. YOLO 추론 (conf=0.5: 확신 50% 이상만 감지)
    results = model(frame, imgsz=320, conf=0.5, verbose=False)

    # 3. 결과 분석
    red_detected = False
    
    for r in results:
        boxes = r.boxes
        for box in boxes:
            # 클래스 이름 확인 (0: Red, 1: Green 등 학습 순서에 따름)
            cls_id = int(box.cls[0])
            cls_name = model.names[cls_id]
            
            x1, y1, x2, y2 = map(int, box.xyxy[0])
            
            # 빨간불이면 정지 플래그 세우기
            if cls_name == "traffic_light_red":
                red_detected = True
                cv2.rectangle(frame, (x1, y1), (x2, y2), (0, 0, 255), 2)
                cv2.putText(frame, "RED", (x1, y1-10), cv2.FONT_HERSHEY_SIMPLEX, 0.5, (0,0,255), 2)
            elif cls_name == "traffic_light_green":
                cv2.rectangle(frame, (x1, y1), (x2, y2), (0, 255, 0), 2)

    # 4. 주행 로직 연결
    if red_detected:
        print("🛑 정지!")
        # bot.Ctrl_Muto(...) 정지 코드
    else:
        print("✅ 주행")
        # 라인트레이싱 코드

    cv2.imshow("YOLO View", frame)
    if cv2.waitKey(1) & 0xFF == ord('q'):
        break

cap.release()
cv2.destroyAllWindows()
```

---

## 5. 성능 최적화 (필수)
YOLO는 Cascade보다 훨씬 무겁습니다. 라즈베리 파이 4에서도 `3~5 FPS` 정도밖에 안 나올 수 있어 자율주행이 버벅거릴 수 있습니다.

1.  **이미지 크기 축소**: `imgsz=320` 또는 `160`까지 줄이세요.
2.  **ONNX 변환**: `.pt` 파일을 `.onnx`로 변환해서 돌리면 2~3배 빨라집니다.
3.  **SKIP FRAME**: 매 프레임마다 YOLO를 돌리지 말고, **3프레임에 1번만** YOLO를 돌리고 나머지는 기존 상태를 유지하세요.

## ⚖️ 비교: Cascade vs YOLO

| 특징            | Haar Cascade (현재 방식) | YOLO (딥러닝 방식)                   |
| :-------------- | :----------------------- | :----------------------------------- |
| **정확도**      | 중 (빛/배경에 민감)      | **최상** (모양, 색상, 맥락까지 이해) |
| **속도**        | **빠름 (30+ FPS)**       | 느림 (3~10 FPS on Pi)                |
| **구현 난이도** | 어려움 (색상 튜닝 필요)  | **쉬움** (데이터만 좋으면 됨)        |
| **라벨링**      | 불필요/간단              | **필수** (노가다 작업 필요)          |

현재의 **Hybrid 방식(Cascade + YCrCb)**이 라즈베리 파이 환경에서는 **속도와 정확도의 균형**이 가장 훌륭할 수 있습니다. YOLO는 정말 정확도가 필요하고 속도를 희생할 수 있을 때 추천합니다.
